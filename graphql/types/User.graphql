type Query{
    user(id: ID!): User!
    users: [User!]!
}

type User{
    id: ID!,
    username: String!
    createdAt: String!
}

type Mutation{
    addUser(data: AddUserInput): User
}

input AddUserInput{
    username: String!
    password: String!
}